---@type LazySpec
local spec = {
    "comfysage/evergarden",
    --lazy = false,
    opts = {
        transparent_background = vim.g.transparent_enabled,
    },
    --cond = false,
    --enabled = false,
    optional = true,
}

return spec
