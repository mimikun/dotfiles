---@type LazySpec
local spec = {
    "yorik1984/newpaper.nvim",
    --lazy = false,
    opts = {
        -- NOTE: Not support "transparent = true"
    },
    --cond = false,
    --enabled = false,
    optional = true,
}

return spec
