---@type LazySpec
local spec = {
    "diegoulloao/neofusion.nvim",
    --lazy = false,
    opts = {
        transparent_mode = vim.g.transparent_enabled,
    },
    --cond = false,
    --enabled = false,
    optional = true,
}

return spec
