---@type LazySpec
local spec = {
    "asperan/chstr.nvim",
    --lazy = false,
    dependencies = {
        "rktjmp/lush.nvim",
    },
    --cond = false,
    --enabled = false,
    optional = true,
}

return spec
