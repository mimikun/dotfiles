---@type LazySpec
local spec = {
    "sontungexpt/witch",
    --lazy = false,
    opts = {
        -- NOTE: Not support "transparent = true"
    },
    --cond = false,
    --enabled = false,
    optional = true,
}

return spec
