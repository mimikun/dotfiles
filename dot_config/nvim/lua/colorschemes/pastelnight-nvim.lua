---@type LazySpec
local spec = {
    "pauchiner/pastelnight.nvim",
    --lazy = false,
    opts = {
        transparent = vim.g.transparent_enabled,
    },
    --cond = false,
    --enabled = false,
    --optional = true,
}

return spec
