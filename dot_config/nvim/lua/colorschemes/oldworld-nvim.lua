---@type LazySpec
local spec = {
    "dgox16/oldworld.nvim",
    --lazy = false,
    opts = {
        -- NOTE: Not support "transparent = true"
    },
    --cond = false,
    --enabled = false,
    optional = true,
}

return spec
