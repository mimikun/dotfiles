---@type LazySpec
local spec = {
    "mimikun/silicon.lua",
    --lazy = false,
    --event = "VimEnter",
    --cmd = "",
    dependencies = { "nvim-lua/plenary.nvim" },
    opts = {
        theme = "GitHub",
        --font = "monospace",
    },
    --cond = false,
}

return spec
