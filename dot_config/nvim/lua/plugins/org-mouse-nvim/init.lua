---@type LazySpec
local spec = {
    "chipsenkbeil/org-mouse.nvim",
    --lazy = false,
    ft = { "org" },
    dependencies = { "nvim-orgmode/orgmode" },
    opts = {},
    --cond = false,
    --enabled = false,
}

return spec
