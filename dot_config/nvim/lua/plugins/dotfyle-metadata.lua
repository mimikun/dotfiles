local cmds = {
    "DotfyleGenerate",
    "DotfyleOpen",
}

return {
    "creativenull/dotfyle-metadata.nvim",
    --lazy = false,
    --event = "VeryLazy",
    cmd = cmds,
    --config = function() end,
    --cond = false,
}
