return {
    "monaqa/dial.nvim",
    --lazy = false,
    event = "VimEnter",
    config = function()
        -- TODO: Write config
        -- https://github.com/monaqa/dial.nvim
    end,
    cond = false,
}
