---@type table
local opts = {
    -- by default, none are enabled
    langs = {
        "python",
        "lua",
    },
    -- paths to emacs packages to additionally load
    load_paths = {},
}

return opts
