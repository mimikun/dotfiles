---@type table
local opts = {
    native_servers = {},
    ui = {
        window = {
            -- "none", "single", "double", "rounded", "solid", "shadow"
            border = "rounded",
        },
    },
}

return opts
