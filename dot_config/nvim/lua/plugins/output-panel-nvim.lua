---@type LazySpec
local spec = {
    "mhanberg/output-panel.nvim",
    --lazy = false,
    cmd = "OutputPanel",
    event = "VeryLazy",
    opts = {},
    --cond = false,
    --enabled = false,
}

return spec
