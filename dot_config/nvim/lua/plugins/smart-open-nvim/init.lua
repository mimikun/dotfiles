---@type LazySpec
local spec = {
    "danielfalk/smart-open.nvim",
    branch = "0.2.x",
    --lazy = false,
    dependencies = require("plugins.smart-open-nvim.dependencies"),
    --cond = false,
    --enabled = false,
}

return spec
