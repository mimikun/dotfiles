---@type LazySpec
local spec = {
    "Bekaboo/deadcolumn.nvim",
    --lazy = false,
    event = "BufEnter",
    opts = {},
    --cond = false,
    -- NOTE: colorcolumn plugin
    enabled = false,
}

return spec
