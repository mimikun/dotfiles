local OPTS = {
    ---@type table
    crnvl96 = {
        window = {
            settings = {
                border = "rounded",
            },
        },
    },
    ---@type table
    mgierada = {
        -- valid options are "single" | "double" | "shadow" | "curved"
        border = "curved",
    },
}

return OPTS
