---@type LazySpec
local spec = {
    "lukas-reineke/indent-blankline.nvim",
    main = "ibl",
    lazy = false,
    --cmd = "CMDNAME",
    --keys = "",
    --event = "VeryLazy",
    --dependencies = { "nvim-tree/nvim-web-devicons" },
    opts = {},
    --config = true,
    --cond = false,
}

return spec
