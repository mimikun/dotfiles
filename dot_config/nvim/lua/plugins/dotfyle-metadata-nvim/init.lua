---@type LazySpec
local spec = {
    "creativenull/dotfyle-metadata.nvim",
    --lazy = false,
    cmd = require("plugins.dotfyle-metadata-nvim.cmds"),
    --cond = false,
    --enabled = false,
}

return spec
