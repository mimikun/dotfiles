---@type LazySpec
local spec = {
    "loctvl842/monokai-pro.nvim",
    --lazy = false,
    opts = {
        transparent_background = vim.g.transparent_enabled,
    },
    --cond = false,
}

return spec
