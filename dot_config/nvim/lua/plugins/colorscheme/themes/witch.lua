-- NOTE: Not support "transparent = true"
---@type LazySpec
local spec = {
    "sontungexpt/witch",
    --lazy = false,
    cmd = "Witch",
    opts = {},
    --cond = false,
    priority = 1000,
}

return spec
