---@type LazySpec
local spec = {
    "bassamsdata/namu.nvim",
    --lazy = false,
    cmd = "Namu",
    keys = require("plugins.namu-nvim.keys"),
    opts = {},
    --cond = false,
    --enabled = false,
}

return spec
