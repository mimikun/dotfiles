---@type SmartTranslate.Config
local opts = {
    default = {
        cmds = {
            source = "auto",
            target = "ja",
            handle = "float",
            engine = "google",
        },
    },
}

return opts
