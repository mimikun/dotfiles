---@type LazySpec
local spec = {
    "tiagovla/tokyodark.nvim",
    --lazy = false,
    opts = {
        transparent_background = vim.g.transparent_enabled,
    },
    --cond = false,
    --enabled = false,
    optional = true,
}

return spec
